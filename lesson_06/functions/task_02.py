def my_sum(x, y):
    return x + y


# можно сначала вызвать функцию, записав её результат в переменную
result = my_sum(1, 4)
# и потом вывести эту переменную
print(result)


# а можно сразу вызвать функцию и вывести результат на экран
print(my_sum(5, 5))
